# 墨洞 InkHole v1.1.0 局域网收尾与文件夹传输 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把手头未提交的界面重构 + 安全加固收尾发布，新增桌面端文件夹传输（拖目录→打包 zip→对端存为 .zip），Mac 用新代码重打包，文档同步后推 GitHub 发 v1.1.0。

**Architecture:** 墨洞两层解耦——`p2p.py` 引擎（mDNS + TCP 直连 + 加密）纯后台可测；`pet.py`/`mainwindow.py`/`inkhole.qml` 是 GUI 面子。文件夹传输在发送侧把目录用 `zipfile` 递归打包成临时 zip，再走现有 `send_file` 全套通道；接收端零改动，当作普通 `.zip` 文件落盘。

**Tech Stack:** Python 3.10+，标准库 `zipfile`/`tempfile`，PySide6（QtWidgets + QML），zeroconf，cryptography，psutil；PyInstaller 打包；GitHub Actions CI。

## Global Constraints

- Python 版本下限：`>=3.10`（`pyproject.toml`）。
- 测试框架：**自定义** `check(name, cond)` + 主入口 `_tests` 列表注册，**不是** pytest 用例风格。新测试函数必须 append 进 `tests/test_p2p.py` 末尾的 `_tests` 列表。运行命令：`make test`（即 `PYTHONPATH=src python tests/test_p2p.py`）。
- 测试节点一律 `enable_mdns=False`（用 `make_node` 辅助函数），手动 `_on_peer_added` 注册对端，绝不碰真实 mDNS。
- 平台守卫：Win11 专属 API（Acrylic 磨砂）必须有 `sys.platform == "win32"` 守卫，非 Windows 回退。
- 文件名 ASCII：GitHub Release 附件产物名用 ASCII（`InkHolePet`），中文名会被剥。
- Git push 必须走代理：`http://127.0.0.1:7897`（抖动则重试）。
- 版本号：本轮 bump 到 `1.1.0`。
- 提交信息用中文，遵循 `type(scope): 描述` 约定（见 git log：`feat`/`fix`/`perf`/`chore`/`docs`）。

---

### Task 1: 回归测试基线 + 补测已有安全加固

确认工作区那批未提交的 `p2p.py` 安全加固（`_safe_filename` / `_unique_path` / 超时）没有破坏现有引擎，并为「不覆盖」新行为补上测试（现有 `test_path_traversal` 可能仍假设旧的覆盖行为）。

**Files:**
- Modify: `tests/test_p2p.py`（末尾主入口 `_tests` 列表 + 新增测试函数）
- 只读参考：`src/inkhole/p2p.py:926-956`（`_safe_filename` / `_unique_path` 定义）

**Interfaces:**
- Consumes: `from inkhole.p2p import ... _MAGIC`（已有导入行，第 39 行），`make_node`、`wait_for_file`、`check`（已有辅助）。
- Produces: 无下游依赖，纯测试增量。

- [ ] **Step 1: 先跑现有测试建立基线**

Run: `make test`
Expected: 末尾打印 `通过: N  失败: 0`（N 为当前项数，约 85）。若有 FAIL，先记录是哪一项——很可能是 `test_path_traversal` 因加固改了行为而失败，这正是本任务要修的。

- [ ] **Step 2: 在 test_p2p.py 末尾（主入口 `if __name__` 之前）追加「不覆盖同名文件」测试**

```python
# ---------- 测试: 同名文件不覆盖(加后缀) ----------
def test_no_overwrite_adds_suffix():
    print("\n=== 测试: 同名文件不覆盖，加 (2) 后缀 ===")
    tmpdir = tempfile.mkdtemp(prefix="inkhole_test_")
    try:
        node_a = make_node(tmpdir, "Alice")
        node_b = make_node(tmpdir, "Bob")
        node_a.start()
        node_b.start()
        time.sleep(0.3)
        node_a._on_peer_added("Bob", "127.0.0.1", node_b.actual_port)
        node_a.select_peer("Bob")

        # 收件箱预置一个同名文件，模拟"已收到过"
        os.makedirs(node_b.cfg.inbox, exist_ok=True)
        existing = os.path.join(node_b.cfg.inbox, "dup.txt")
        with open(existing, "w", encoding="utf-8") as f:
            f.write("原有内容")

        src = os.path.join(tmpdir, "dup.txt")
        with open(src, "w", encoding="utf-8") as f:
            f.write("新发来的内容")
        check("发送成功", node_a.send_file(src))

        got = wait_for_file(node_b.cfg.inbox, "dup (2).txt")
        check("同名文件落为 dup (2).txt", got is not None)
        with open(existing, encoding="utf-8") as f:
            check("原有文件未被覆盖", f.read() == "原有内容")
    finally:
        node_a.stop(); node_b.stop()
        shutil.rmtree(tmpdir, ignore_errors=True)
```

- [ ] **Step 3: 在 test_p2p.py 末尾追加「非法文件名清洗」测试**

```python
# ---------- 测试: 非法文件名清洗 ----------
def test_illegal_filename_sanitized():
    print("\n=== 测试: 非法文件名清洗 ===")
    from inkhole.p2p import _safe_filename
    check("裁掉正斜杠路径", _safe_filename("a/b/c.txt") == "c.txt")
    check("裁掉反斜杠路径", _safe_filename("a\\b\\c.txt") == "c.txt")
    check("替换 NTFS 非法字符", _safe_filename('a:b*c?.txt') == "a_b_c_.txt")
    check("去掉尾部点和空格", _safe_filename("name.  ") == "name")
    check("纯路径穿越回退 unknown", _safe_filename("../..") == "unknown")
    check("空名回退 unknown", _safe_filename("") == "unknown")
```

- [ ] **Step 4: 把两个新测试注册进主入口 `_tests` 列表**

在 `tests/test_p2p.py` 主入口的 `_tests = [ ... ]` 列表末尾（`test_mdns_rebuild,` 之后）加两行：

```python
        test_mdns_rebuild,
        test_no_overwrite_adds_suffix,
        test_illegal_filename_sanitized,
    ]
```

- [ ] **Step 5: 若 test_path_traversal 因新行为失败，修正它**

只有 Step 1 显示 `test_path_traversal` FAIL 时才做。打开该函数，把「断言收件箱里出现被裁剪名的文件」的期望对齐到新行为（`_safe_filename` 已把 `../` 裁成 basename，落盘仍在收件箱内；若旧断言写死了覆盖语义则改成新的唯一名）。不改动 `p2p.py`，只改测试期望。

- [ ] **Step 6: 跑全套测试验证全绿**

Run: `make test`
Expected: `通过: N+新增项  失败: 0`。

- [ ] **Step 7: 提交**

```bash
git add tests/test_p2p.py
git commit -m "test(p2p): 补测同名不覆盖与非法文件名清洗，对齐安全加固后的行为"
```

---

### Task 2: 提交已有安全加固（p2p.py）

`p2p.py` 那 +58 行加固已被 Task 1 的测试覆盖，单独成一个清晰 commit。

**Files:**
- Commit only: `src/inkhole/p2p.py`（工作区已有改动，不再编辑）

**Interfaces:**
- Consumes: 无。
- Produces: `_safe_filename(raw:str)->str`、`_unique_path(directory:str, filename:str)->str` 供全项目使用；`_RECV_IDLE_TIMEOUT`/`_SEND_IO_TIMEOUT`/`_MAX_WHE1_SIZE` 常量。

- [ ] **Step 1: 确认 p2p.py 改动内容**

Run: `git diff src/inkhole/p2p.py --stat`
Expected: 显示 `src/inkhole/p2p.py | 58 +...`（约 58 行增改）。

- [ ] **Step 2: 提交**

```bash
git add src/inkhole/p2p.py
git commit -m "fix(p2p): 文件名清洗+同名不覆盖+收发超时,加固接收路径"
```

---

### Task 3: 真机验证并提交界面重构（mainwindow.py + pet.py）

新主界面动了核心交互却从未运行。本任务先在本机（Windows，有 GUI）实际起进程验证不崩、能发文件，再提交。

**Files:**
- Commit: `src/inkhole/mainwindow.py`（新增，未跟踪）、`src/inkhole/pet.py`（−67/+123）
- 只读参考：`src/inkhole/pet.py:851-863`（MainWindow 构造与 ctl 字典注入）

**Interfaces:**
- Consumes: `Bridge`（`pet.py` 内），`P2PNode`（`p2p.py`）。
- Produces: `MainWindow(bridge, ctl:dict, icon=None)`，ctl 键：`pet_visible`/`set_pet_visible`/`is_autostart`/`set_autostart`/`apply_settings`。

- [ ] **Step 1: 语法自检（导入不执行 GUI）**

Run: `PYTHONPATH=src python -c "import ast; ast.parse(open('src/inkhole/mainwindow.py',encoding='utf-8').read()); ast.parse(open('src/inkhole/pet.py',encoding='utf-8').read()); print('OK')"`
Expected: `OK`（无 SyntaxError）。

- [ ] **Step 2: 真机启动主界面（后台运行，8 秒后自动退出）**

Run:
```bash
PYTHONPATH=src python -c "import inkhole.pet as p, threading, sys; from PySide6.QtCore import QTimer; sys.argv=['pet','--name','验证机']; import PySide6.QtWidgets as W; \
_orig=W.QApplication.exec; \
print('启动中...'); \
threading.Timer(8, lambda: __import__('os')._exit(0)).start(); p.main([])" 2>&1 | tail -20
```
Expected: 无 traceback，日志出现 `墨洞已开启`；窗口弹出（你肉眼确认玻璃拟态主窗口 + 中央墨洞动画）。8 秒后进程自杀退出。
若报 `QML 加载失败` 或 `ImportError`，停下排查——不要带病提交。

- [ ] **Step 3: 肉眼验收清单（手动，逐项确认）**

启动后确认：① 主窗口显示，标题栏「墨洞 InkHole」+ 最小化/关闭；② 中央墨洞动画在转；③ 右侧「设备」面板显示「正在搜索局域网设备…」；④ 点齿轮进设置页，设备名/口令/端口/收件箱字段可见；⑤ 点「返回」回主页。任意一项异常则记录并修复后重来。

- [ ] **Step 4: 提交界面重构**

```bash
git add src/inkhole/mainwindow.py src/inkhole/pet.py
git commit -m "feat(ui): 新增玻璃拟态主界面,设置从右键菜单迁入应用内"
```

---

### Task 4: 文件夹打包核心函数（TDD）

把「目录→临时 zip」抽成纯函数 `_zip_dir`，可单测、不依赖 GUI。这是文件夹传输的地基。

**Files:**
- Modify: `src/inkhole/p2p.py`（在「文件名安全」区块附近新增 `_zip_dir`）
- Test: `tests/test_p2p.py`（新增测试 + 注册）

**Interfaces:**
- Consumes: 标准库 `zipfile`、`tempfile`、`os`（`p2p.py` 已 import os）。
- Produces: `_zip_dir(src_dir: str) -> str` —— 把 `src_dir` 递归打包成一个临时 zip，返回临时 zip 的绝对路径（文件名为 `<目录basename>.zip`）。调用方负责发送后删除该临时文件及其父临时目录。空目录返回一个合法的空 zip。

- [ ] **Step 1: 写失败测试**

在 `tests/test_p2p.py` 末尾追加：

```python
# ---------- 测试: 目录打包成 zip ----------
def test_zip_dir_roundtrip():
    print("\n=== 测试: 目录打包 zip 往返 ===")
    import zipfile
    from inkhole.p2p import _zip_dir
    tmpdir = tempfile.mkdtemp(prefix="inkhole_test_")
    try:
        # 造一个带子目录、中文名、空文件的源目录
        src = os.path.join(tmpdir, "我的资料")
        os.makedirs(os.path.join(src, "子目录"))
        with open(os.path.join(src, "a.txt"), "w", encoding="utf-8") as f:
            f.write("内容A")
        with open(os.path.join(src, "子目录", "b.txt"), "w", encoding="utf-8") as f:
            f.write("内容B")
        open(os.path.join(src, "空.dat"), "w").close()

        zip_path = _zip_dir(src)
        check("返回的是 .zip 路径", zip_path.endswith(".zip"))
        check("zip 文件名是目录名", os.path.basename(zip_path) == "我的资料.zip")
        check("zip 文件已生成", os.path.isfile(zip_path))

        # 解压回来逐一核对
        out = os.path.join(tmpdir, "out")
        with zipfile.ZipFile(zip_path) as z:
            z.extractall(out)
        with open(os.path.join(out, "a.txt"), encoding="utf-8") as f:
            check("a.txt 内容一致", f.read() == "内容A")
        with open(os.path.join(out, "子目录", "b.txt"), encoding="utf-8") as f:
            check("子目录/b.txt 内容一致", f.read() == "内容B")
        check("空文件保留", os.path.isfile(os.path.join(out, "空.dat")))
    finally:
        shutil.rmtree(tmpdir, ignore_errors=True)


# ---------- 测试: 空目录打包不报错 ----------
def test_zip_dir_empty():
    print("\n=== 测试: 空目录打包 ===")
    import zipfile
    from inkhole.p2p import _zip_dir
    tmpdir = tempfile.mkdtemp(prefix="inkhole_test_")
    try:
        src = os.path.join(tmpdir, "空目录")
        os.makedirs(src)
        zip_path = _zip_dir(src)
        check("空目录也生成合法 zip", os.path.isfile(zip_path))
        with zipfile.ZipFile(zip_path) as z:
            check("zip 内无文件条目", len([n for n in z.namelist() if not n.endswith("/")]) == 0)
    finally:
        shutil.rmtree(tmpdir, ignore_errors=True)
```

- [ ] **Step 2: 注册进 `_tests` 列表**

```python
        test_zip_dir_roundtrip,
        test_zip_dir_empty,
    ]
```

- [ ] **Step 3: 跑测试确认失败**

Run: `make test`
Expected: FAIL —— `ImportError: cannot import name '_zip_dir'` 或对应 AssertionError。

- [ ] **Step 4: 实现 `_zip_dir`**

在 `src/inkhole/p2p.py` 的 `_unique_path` 函数之后新增（`import tempfile` 加到文件顶部 import 区，`os` 已有）：

```python
def _zip_dir(src_dir: str) -> str:
    """把目录递归打包成一个临时 zip，返回临时 zip 绝对路径。

    zip 名为 <目录basename>.zip，落在独立临时目录里（调用方发送后应删除
    整个临时目录）。arcname 用相对 src_dir 的路径，保持目录结构；空目录
    也生成合法（空）zip。用 ZIP_DEFLATED 压缩。
    """
    import zipfile
    src_dir = os.path.abspath(src_dir)
    base = os.path.basename(src_dir.rstrip("/\\")) or "folder"
    tmp_root = tempfile.mkdtemp(prefix="inkhole_zip_")
    zip_path = os.path.join(tmp_root, base + ".zip")
    with zipfile.ZipFile(zip_path, "w", zipfile.ZIP_DEFLATED) as z:
        for root, _dirs, files in os.walk(src_dir):
            for fn in files:
                full = os.path.join(root, fn)
                arc = os.path.relpath(full, src_dir)
                z.write(full, arc)
    return zip_path
```

在文件顶部 import 区（`import uuid` 附近）确认加入 `import tempfile`。

- [ ] **Step 5: 跑测试确认通过**

Run: `make test`
Expected: `失败: 0`，新增 `test_zip_dir_roundtrip` / `test_zip_dir_empty` 全 PASS。

- [ ] **Step 6: 提交**

```bash
git add src/inkhole/p2p.py tests/test_p2p.py
git commit -m "feat(p2p): 新增 _zip_dir 目录递归打包为临时 zip"
```

---

### Task 5: 发送队列支持目录（临时 zip 生命周期）

让 `SendQueue`/发送路径识别目录：目录先 `_zip_dir` 打包，发临时 zip，发完删临时目录。集中在 `pet.py` 的发送入口，保证 QML 桌宠和主窗口两条拖拽路径都受益。

**Files:**
- Modify: `src/inkhole/pet.py`（`Bridge.dropFile` 第 666-675 行区域，+ 新增一个打包辅助）
- Test: `tests/test_p2p.py`（用 SendQueue 端到端验证目录发送）

**Interfaces:**
- Consumes: `_zip_dir`（Task 4），`SendQueue`（`pet.py:133`），`node.send_file`。
- Produces: `Bridge.dropFile(url)` 接受目录 url；新增内部方法 `Bridge._enqueue_path(path:str)` 统一处理文件/目录入队与临时清理。

- [ ] **Step 1: 写端到端测试（引擎层，不经 QML）**

在 `tests/test_p2p.py` 末尾追加。此测试直接用 `_zip_dir` + `send_file` 验证「目录→zip→对端收到 .zip→解压内容一致」，覆盖发送侧真实链路：

```python
# ---------- 测试: 文件夹发送端到端(打包 zip 送达) ----------
def test_folder_send_end_to_end():
    print("\n=== 测试: 文件夹发送端到端 ===")
    import zipfile
    from inkhole.p2p import _zip_dir
    tmpdir = tempfile.mkdtemp(prefix="inkhole_test_")
    try:
        node_a = make_node(tmpdir, "Alice")
        node_b = make_node(tmpdir, "Bob")
        node_a.start(); node_b.start()
        time.sleep(0.3)
        node_a._on_peer_added("Bob", "127.0.0.1", node_b.actual_port)
        node_a.select_peer("Bob")

        src = os.path.join(tmpdir, "项目")
        os.makedirs(src)
        with open(os.path.join(src, "readme.txt"), "w", encoding="utf-8") as f:
            f.write("hello folder")

        zip_path = _zip_dir(src)
        check("发送临时 zip 成功", node_a.send_file(zip_path))

        got = wait_for_file(node_b.cfg.inbox, "项目.zip")
        check("对端收到 项目.zip", got is not None)
        with zipfile.ZipFile(got) as z:
            check("zip 内含 readme.txt", "readme.txt" in z.namelist())
    finally:
        node_a.stop(); node_b.stop()
        shutil.rmtree(tmpdir, ignore_errors=True)
```

注册进 `_tests`：

```python
        test_folder_send_end_to_end,
    ]
```

- [ ] **Step 2: 跑测试确认通过（验证 Task 4 的函数在真实发送链路可用）**

Run: `make test`
Expected: `test_folder_send_end_to_end` PASS（`_zip_dir` + `send_file` 已具备，此测试应直接过；若失败说明打包/发送链路有问题，需修）。

- [ ] **Step 3: 在 pet.py 新增 `_enqueue_path`，改写 `dropFile`**

把 `pet.py` 的 `dropFile`（约 666-675 行）改为：

```python
        @Slot(str)
        def dropFile(self, url: str):
            """QML DropArea / 主窗口拖入的 url：文件直接入队，目录先打包成 zip。
            队列单线程串行发送：一次拖 N 项不会开 N 个并发连接。"""
            path = QUrl(url).toLocalFile() if url.startswith("file:") else url
            if not path:
                return
            if not self.node.selected_peer():
                self.status.emit("右键选择目标设备")
                return
            self._enqueue_path(path)

        def _enqueue_path(self, path: str):
            """文件直接入队；目录打包成临时 zip 再入队,发送完成后清理临时目录。"""
            if os.path.isfile(path):
                self._sendq.put(path)
                return
            if os.path.isdir(path):
                from .p2p import _zip_dir
                try:
                    self.status.emit("正在打包文件夹…")
                    zip_path = _zip_dir(path)
                except Exception as e:
                    self.status.emit(f"打包失败：{e}")
                    return
                self._pending_zip_dirs.append(os.path.dirname(zip_path))
                self._sendq.put(zip_path)
```

- [ ] **Step 4: 初始化临时目录清理列表 + 发送后清理**

在 `Bridge.__init__`（`pet.py:589` 起）里，`self._recent` 那行附近加：

```python
            self._pending_zip_dirs: list[str] = []   # 待清理的临时打包目录
```

并把 `SendQueue` 构造的 `on_batch_done` 回调改为在批次结束后清理临时 zip 目录：

```python
            self._sendq = SendQueue(
                lambda p: self.node.send_file(p),
                on_batch_done=lambda ok, total: self._on_batch_done(ok, total),
            )
```

新增方法：

```python
        def _on_batch_done(self, ok: int, total: int):
            """一批发送结束：清理临时打包目录 + 多文件时聚合提示。"""
            import shutil as _sh
            for d in self._pending_zip_dirs:
                _sh.rmtree(d, ignore_errors=True)
            self._pending_zip_dirs = []
            if total > 1:
                self.status.emit(f"已吞入 {ok}/{total} 个")
```

- [ ] **Step 5: 主窗口 dropEvent 放行目录**

`src/inkhole/mainwindow.py` 的 `dropEvent`（约 702-705 行）当前直接把每个 url 转交 `dropFile`，已能处理目录（判定在 `_enqueue_path` 内）。确认它不再有 `isfile` 预过滤。当前代码：

```python
    def dropEvent(self, e):
        for url in e.mimeData().urls():
            self._bridge.dropFile(url.toString())
        e.acceptProposedAction()
```

无需改动——`dropFile` 已统一处理。仅确认此处没有多余的 `isfile` 守卫。

- [ ] **Step 6: 语法自检 + 跑测试**

Run: `PYTHONPATH=src python -c "import ast; ast.parse(open('src/inkhole/pet.py',encoding='utf-8').read()); print('OK')" && make test`
Expected: `OK` 且 `失败: 0`。

- [ ] **Step 7: 真机验证拖文件夹发送（本机手动）**

起两个节点（或一个节点对自己回环不便，用本机 + 手机/另一台）。最小验证：本机起 `PYTHONPATH=src python -m inkhole.pet`，拖一个小文件夹进主窗口，确认状态栏出现「正在打包文件夹…」且不崩溃。收端确认收到 `<目录名>.zip`。

- [ ] **Step 8: 提交**

```bash
git add src/inkhole/pet.py tests/test_p2p.py
git commit -m "feat(pet): 拖文件夹自动打包成 zip 发送,发送后清理临时文件"
```

---

### Task 6: 文档一致性修复 + 版本号 bump

修掉文档与代码已经不一致的地方（覆盖→不覆盖、不能拖文件夹→自动打包），补新功能说明，版本号升到 1.1.0。

**Files:**
- Modify: `README.md`、`docs/inkhole-墨洞文件传输.md`、`docs/墨洞项目计划.md`、`pyproject.toml`

**Interfaces:**
- Consumes: 无。Produces: 无（纯文档）。

- [ ] **Step 1: pyproject.toml 版本号**

`src/inkhole` 的版本在 `pyproject.toml:3`。改：

```toml
version = "1.1.0"
```

- [ ] **Step 2: README.md 修正「覆盖」表述**

`README.md` 第 130 行附近「同名文件：桌面端直接覆盖（新版本替掉旧版本）」改为：

```markdown
同名文件：收件箱已有同名文件时自动加 " (2)" 后缀，绝不覆盖已有文件（桌面端与 Android 端一致）。传输中断的半截文件不会落盘、不会覆盖已有文件。
```

- [ ] **Step 3: README.md 修正「不能拖文件夹」+ 补 Features**

第 7-9 行摘要「无需服务器」段保留。Features 列表（第 32-46 行）新增两条：

```markdown
- **文件夹传输**：桌面端拖入文件夹自动递归打包成 `<文件夹名>.zip` 发送，对端收到即为一个 zip 文件（不自动解压，规避路径穿越）
- **应用内主界面**：玻璃拟态主窗口——设备列表、点击/拖拽发送、应用内设置页、最近接收；桌宠挂件保留为可开关选项
```

「常见问题」里若有「不能拖文件夹（先压缩成 zip）」的表述，改为「拖文件夹会自动打包成 zip 发送」。检查 `docs/inkhole-墨洞文件传输.md:146` 的「不能拖文件夹（先压缩成 zip）」同步改。

- [ ] **Step 4: docs 两份文档同步**

`docs/墨洞项目计划.md`：把「待办」里的「文件夹传输（递归打包或原生递归）」从 `[ ]` 勾成 `[x]`（桌面端），并在「当前版本」段更新版本号与「同名覆盖」表述（改为不覆盖加后缀）。`docs/inkhole-墨洞文件传输.md` 顶部若有版本号一并更新。

- [ ] **Step 5: 提交**

```bash
git add README.md docs/inkhole-墨洞文件传输.md docs/墨洞项目计划.md pyproject.toml
git commit -m "docs: 同步文件夹传输/新界面/不覆盖行为,版本号 bump 到 1.1.0"
```

---

### Task 7: 发布 v1.1.0（打 tag 触发 CI，Mac 随之重打包）

Mac 与 Windows 同源，`package.yml` 已含 macos-latest 构建槽 + spec 已含 darwin 分支。推 tag 即触发双端构建并发布 Release。

**Files:**
- 只读：`.github/workflows/package.yml`、`packaging/inkhole-pet.spec`（确认无需改动）

**Interfaces:** 无代码接口。

- [ ] **Step 1: 确认 CI 与 spec 已就绪（只读检查）**

确认 `.github/workflows/package.yml` 有 `macos-latest` 矩阵项（第 21-23 行）且 spec 有 `if sys.platform == "darwin": BUNDLE(...)` 分支（`inkhole-pet.spec:80`）。两者都在——无需改动。

- [ ] **Step 2: 确认所有改动已提交、工作区干净**

Run: `git status --short`
Expected: 空输出（无未提交改动）。若有残留，回到对应任务补提交。

- [ ] **Step 3: 推送 main 分支（走代理）**

```bash
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin main
```
Expected: 推送成功。抖动报错则重试同一命令。

- [ ] **Step 4: 打 v1.1.0 tag 并推送**

```bash
git tag v1.1.0
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push origin v1.1.0
```
Expected: tag 推送成功，触发 `package.yml`（`on: push: tags: v*`）。

- [ ] **Step 5: 观察 CI 构建结果**

Run: `gh run list --workflow=package.yml --limit 3`
（gh 需走代理：`gh` 读环境变量 `HTTPS_PROXY`；若失败则 `HTTPS_PROXY=http://127.0.0.1:7897 gh run list --workflow=package.yml --limit 3`）
Expected: 最新一条对应 `v1.1.0`，状态最终为 `completed / success`，产出 Windows zip + macOS .app zip 并发布到 v1.1.0 Release。若某端失败，取该 job 日志排查（常见：macOS PyInstaller 依赖），修复后删除 tag 重打。

---

## Self-Review 结果

**Spec 覆盖检查**（逐节对照 `2026-07-11-inkhole-v1.1.0-lan-finalize-design.md`）：
- 二.收尾新主界面 → Task 3 ✓
- 三.文件夹传输 → Task 4（打包）+ Task 5（队列/UI 接入）✓
- 三.测试(TDD) → Task 4 Step1-3、Task 5 Step1 ✓
- 四.已有安全加固测试 → Task 1 ✓
- 五.文档一致性 → Task 6 ✓
- 六.执行顺序 → Task 1→7 严格对应（先测试基线→加固提交→界面→打包→队列→文档→发布）✓
- 七.验收标准 → Task 6/7 覆盖 ✓
- Mac 同步打包 → Task 7 ✓

**占位符扫描**：无 TBD/TODO；每个代码步给出完整代码。✓

**类型一致性**：`_zip_dir(src_dir)->str` 在 Task 4 定义、Task 5 消费，签名一致；`_enqueue_path`/`_on_batch_done`/`_pending_zip_dirs` 在 Task 5 内自洽；ctl 字典键与 `pet.py`/`mainwindow.py` 现有实现一致。✓

**已知待实现决策**：Task 5「发送文件夹按钮入口」在 spec 中标为可选，本计划以拖拽为主路径，未强制加按钮——符合 spec。
