# 墨洞(InkHole)文件传输 —— 项目计划与进度

> 最后更新：2026-07-15
> 一句话目标：把文件拖进桌面或手机上的「墨洞」，文件自动出现在另一台设备上。

## 总体设计

- 网络环境：**同一局域网**（mDNS 自动发现）；**跨网络**由「手动添加设备」承载——填对方 IP 直连，配合 Tailscale 等虚拟局域网即可异地互传
- 传输方式：**P2P 直连**（mDNS 发现/手动登记 + TCP 直传，无需自建服务器）
- 平台：**Windows / macOS / Android 三端互通**
- 加密：可选 AES-256-GCM 端到端加密（口令两端一致）

架构（两层解耦）：

```
   电脑A / Android                              电脑B / Android
┌──────────────┐    mDNS 自动发现     ┌──────────────┐
│ 墨洞桌宠/App  │ ──────────────────> │ 墨洞桌宠/App  │
│  (拖入/发送)  │    TCP 直连传输      │  (收到文件)   │
│              │    WHPP 协议         │              │
└──────────────┘    双向互传           └──────────────┘
   面子:桌面主窗口(QtWidgets)+桌宠(QML) / Compose UI
   里子:P2P引擎(mDNS+TCP直连+WHE2加密)
```

## 已完成 ✅（2026-07-03：P2P 引擎 + 桌面桌宠）

- [x] **P2P 引擎 `p2p.py`**：mDNS（zeroconf）注册 `_inkhole._tcp.local.` + TCP 直连；WHPP 协议；手动选目标设备，离线清空选中不自动切换；可选端到端加密（复用 crypto.py）；命令行入口。
- [x] **加密模块 `crypto.py`**：AES-256-GCM，PBKDF2-HMAC-SHA256（10 万次迭代）密钥派生；WHE1 整块格式（小文件）。
- [x] **桌宠挂件 `pet.py` + `inkhole.qml`**：PySide6+QML，黑洞造型，拖文件触发吸入/吐出动画，右键菜单发送目标子菜单，开机自启（Windows/macOS/Linux）。
- [x] **测试 `test_p2p.py`**：7 组 28 项，TCP 直连/加密/设备切换/离线/回调/多文件/路径穿越。
- [x] **修复 Windows 端口绑定 bug**：`bind(("", 0))` 让 OS 自动分配唯一端口，防两节点同机分到同一端口。

## 已完成 ✅（2026-07-03：Android 客户端）

- [x] **P2P 引擎 `InkHoleNode.kt`**：Android NSD 发现 + TCP 直连；resolve 串行队列解决 `FAILURE_ALREADY_ACTIVE`。
- [x] **`WHPP.kt` + `Crypto.kt`**：协议与加密与桌面版逐字节兼容。
- [x] **UI**：Jetpack Compose 暗色主题，设备列表 + 文件选择(SAF) + 收件箱 + 设置。
- [x] **三端互通**：Windows / macOS / Android 同一协议，连同一 WiFi 即可互传。

## 已完成 ✅（2026-07-04：品牌重塑 + 引擎第二轮）

### 品牌：墨洞 InkHole
- [x] 三端 UI 文案/托盘/通知全部替换；桌面图标重绘（墨黑核心+青色视界环）；Android 矢量自适应图标。
- [x] 桌面 QML 重设计：墨黑核心 + 青色吸积弧双层反向旋转 + 呼吸光晕；传输进度环；碎片动画青玻璃质感。
- [x] Android Compose UI 重设计：墨洞英雄区（旋转吸积弧/呼吸/传输进度环）、设备横排芯片、接收历史卡片（持久化 50 条）。
- [x] 内部标识全量更名：Python 包名 `inkhole`，mDNS 服务类型 `_inkhole._tcp`，Android 包名 `com.rexvane.inkhole`，类名/文件名/配置路径全部同步。与 v1.0.0 不互通。

### 引擎第二轮
- [x] **WHE2 分块加密**：>32MB 加密文件走 4MB 分块流式 AES-GCM，块序号进 nonce+AAD（防篡改/重排），收发内存峰值恒定 4MB。≤32MB 保持 WHE1 整块。
- [x] **ACK 回执**：header 加 `want_ack`，接收方落盘后回 1 字节；发送方能感知解密失败/磁盘问题；向后兼容老版本。
- [x] **传输进度**：`on_progress(kind, name, done, total)` 回调，节流 0.25s；桌宠/Android 同步显示百分比。
- [x] **发送队列**：桌面拖 N 个文件串行排队，批量完成聚合提示"已吞入 k/N"。
- [x] **仅接收目标设备（trusted_only）**：只接受选中设备 IP 的连接，桌面/Android 设置页均可切换并持久化。
- [x] **多网卡/VPN**：mDNS 宣告全部本机 IPv4（默认路由优先），对端逐地址尝试连接。
- [x] **拒收 drain**：拒收时先消化对端已发数据（上限 8MB）再回执关闭，ACK_FAIL 不被 RST 冲掉。

## 已完成 ✅（2026-07-04：可靠性与安全修复）

- [x] **半截文件不落盘**：`remaining==0` 才原子 `os.replace` 改名，否则删 `.part`（桌面+Android）。
- [x] **恶意 size 拒收**：非整数/负数/超 1TB 拒收；磁盘余量不足（<256MB）拒收。
- [x] **mDNS 名字冲突根治**：服务名带 8 位唯一实例 ID；TXT `instance_id` 做自我过滤；离线按服务名精确匹配；显示名撞名加 " (2)" 后缀。
- [x] **Android NSD 稳定性**：resolve 串行队列；`stop()` 注销加 try-catch；`onServiceRegistered` 记录系统实际注册名。
- [x] **设置持久化**：非敏感设置使用桌面 `config.json`（Win `%APPDATA%\InkHole`，Mac `~/Library/Application Support/InkHole`）与 Android `SharedPreferences("inkhole")`；共享传输口令进入系统凭据库 / Android Keystore，旧明文字段自动迁移清除；CLI 参数改一次记住。
- [x] **前台服务**：Android P2P 节点挪进 `InkHoleService`，锁屏/切后台不断线。
- [x] **系统分享入口**：Android ACTION_SEND/ACTION_SEND_MULTIPLE，任意 App 分享→墨洞。
- [x] **收件箱可见**：收到的文件导出到系统 `Downloads/InkHole`（MediaStore），系统通知可点击打开。
- [x] **Windows 默认收件箱**：`SHGetKnownFolderPath` 查询真实桌面，兼容 OneDrive 重定向。
- [x] **打包/CI 修复**：产物使用 ASCII 命名（`InkHolePet-windows.zip` / `InkHole-v*.apk` / `InkHolePet-macos.zip`），三端分开发布。
- [x] **测试**：17 组 66 项全通过（兼容 pytest）。

## 已完成 ✅（2026-07-05：正式发布 + 幽灵设备修复）

- [x] **v1.0.0 / v1.0.1 发布**：GitHub Release 三端产物（Windows zip / macOS zip / Android APK），tag 推送自动触发 CI 构建上传。
- [x] **Windows 打包改 onedir+zip**：v1.0.0 的 onefile 运行时解压 DLL 到 %TEMP% 被 Defender 拦截；onedir 就地运行绕开（macOS 保持 onefile+BUNDLE）。
- [x] **幽灵设备修复（v1.0.2）**：对端崩溃/强杀不发 mDNS goodbye、WiFi 下 goodbye 也常丢，列表条目要等 PTR TTL(约75分钟)才消失。新增存活探测线程：每 10s TCP 探活，连续 2 轮失败剔除并刷新菜单，选中设备离线同步取消选中。
- [x] **README 常见问题章节**：SmartScreen/防火墙/macOS Gatekeeper/找不到设备排查/Issue 反馈模板。
- [x] **Release Notes 补全**：v1.0.1（打包修复说明）、v1.0.2（幽灵设备修复说明）。
- [x] **Windows 11 真机验证**：完整测试套件 18 组 70 项全通过；Release 包下载解压实测正常启动。
- [x] **仓库 Topics 标签**：p2p / file-transfer / lan / mdns / android / pyside6。
- [x] **测试**：新增第 18 组"幽灵设备探测剔除"，共 18 组 70 项全通过（兼容 pytest）。

## 已完成 ✅（2026-07-11：v1.1.0 局域网收尾）

- [x] **应用内主界面 `mainwindow.py`**：设备列表、点击/拖拽发送、应用内设置页、最近接收；设备名/口令/收件箱/自启等设置从右键菜单迁入主界面；桌宠挂件保留为可开关选项。
- [x] **文件夹传输（桌面端）**：拖文件夹自动递归打包成 `<文件夹名>.zip` 发送（保留完整目录结构含空子目录），对端收到即一个 zip 文件，不自动解压（规避路径穿越）；Android 端可正常接收。
- [x] **接收路径安全加固 `p2p.py`**：同名文件加 " (2)" 后缀不覆盖（`_unique_path`）；文件名清洗（非法字符 / NTFS ADS）；收发超时。
- [x] **测试**：29 组 123 项全通过（新增同名不覆盖、非法文件名清洗、目录打包往返、文件夹发送、队列按文件清理）。

## 已完成 ✅（2026-07-13：Windows 桌面体验重构）

- [x] **视觉骨架**：主窗口改为中性深色双栏工作台，左侧发送区与右侧设备/最近接收信息栏职责稳定；默认 `960×640`，最小 `800×580`。
- [x] **控件一致性**：移除 emoji 设备图标，统一 8px 圆角、固定控件尺寸、设备/文件卡片层级和青绿/暖金状态色。
- [x] **流畅度**：墨洞改为时间驱动 60fps，窗口或主页隐藏时停表；加入卡片悬停、动画开关、页面淡入、状态淡入、拖拽反馈和主窗口传输进度环。
- [x] **响应边界**：设备名、文件名、状态文本使用省略策略，Windows 125% 缩放下完成主页、设置页、进度态、最小尺寸和长文本渲染检查。
- [x] **功能边界**：继续使用原 Bridge 信号、设置回调、设备选择、拖放和发送队列，没有改动 P2P、加密或落盘逻辑。
- [x] **验证**：`pytest` 29 项通过，Python 全量编译通过；README 与当前使用/打包/Android 文档同步更新。
- [x] **发布一致性**：README 增加 Windows 主界面截图，全部 Markdown 标记当前/归档状态；Android 非 tag 构建从 Gradle `versionName` 读取 APK 名称。

## 已完成 ✅（2026-07-14：v1.2.0 统一引擎、手动直连与缺陷修复）

- [x] **架构决策**：跨网络方案从「SSH 服务器中转」改为「手动添加设备 + Tailscale 直连」——零服务器、零额外部署、速度为两端直连带宽。SSH 中转全套实现（桌面 `ssh_relay.py`/`relay_crypto.py` + Android `relay/` + 协议文档）保全在 `wip/ssh-relay-android` 分支，主线不再引用。
- [x] **界面简化**：移除「局域网 / 跨网络」模式切换，单一传输引擎、单一设备列表；设置页合并为一页。
- [x] **手动添加设备**：设置页填对方 IP+端口即入列表；持久化到 `config.json`；启动即注册；探测线程离线剔除、回线自动恢复（mDNS 不覆盖的网络靠它兜底）。
- [x] **缺陷修复（16 项）**：主窗口引用未定义 bridge 信号导致构造崩溃；节点重启占用 UI 线程造成的界面冻结（改后台线程 + 串行门）；只改收件箱不落盘；页面切换动画残留半透明；退出卡顿（限时 2s）；mDNS 重建与 stop() 竞态（幽灵设备/实例泄漏）；设置分段按钮高亮残留等。
- [x] **品牌资产**：`assets/` 图标（png/ico/icns）+ `branding.py` 共用绘制 + `generate-icons.py` 生成脚本。
- [x] **测试**：新增手动设备集成测试与主窗口离屏冒烟测试（QT_QPA_PLATFORM=offscreen），`pytest` 37 项全通过。
- [x] **v1.2.0 发布**：三平台产物（Windows zip / macOS zip / Android APK）经 tag 触发 CI 构建上传;Android 本版为 v1.1.0 功能集 + 版本对齐。

## 已完成 ✅（2026-07-15：v1.3.0 三端体验对齐）

- [x] **Android 手动添加设备**：设置内填 IP+端口，跨网（Tailscale）直连与桌面对齐；探活循环离线剔除/回线自动恢复；持久化 SharedPreferences。
- [x] **修复「仅接收目标设备」误拒**：桌面多网卡/VPN(如 Clash TUN)时连接源 IP 不是 NSD 解析到的那一个,Android 按单 IP 比对把自己人拒收、且对端显示"口令不一致"误导排查。桌面 mDNS TXT 新增 `ips`(全部本机 IPv4),Android 按完整列表放行;双端拒收判断均移到协议头之后,存活探测空连接不再刷"已拒收"。
- [x] **IP 输入自动纠正**（双端）：全角句号/逗号/空格转点；缺分隔符唯一合法拆分自动补全（100127.46.26 → 100.127.46.26），有歧义拒绝。
- [x] **Android 品牌图标对齐桌面**：墨黑核心+青外弧+暖金内弧同款图案（自适应 + v33 单色 + 通知小图标），尺寸形状不变。
- [x] **桌面墨洞视觉对齐 Android**：径向渐变/双层反向旋转弧/呼吸/雷达波纹/进度环逐参数一致，尺寸与点击行为不变。
- [x] **测试**：pytest 38 项全通过；Android assembleDebug 本地构建通过。

## 已完成 ✅（2026-07-15–19：v1.3.1–v1.3.30 实测反馈打磨）

- [x] **桌宠多屏修复（v1.3.1）**：贴边/收起计算混用全局坐标与所在屏尺寸导致副屏错乱，全部改屏内坐标；右键菜单拆分「关闭桌宠」（持久化）与「退出程序」，托盘「退出」退全部。
- [x] **IP 实时分段输入（v1.3.1）**：双端边打边自动分段（段满 3 位或再加一位超 255 自动落点，1001274626 → 100.127.46.26）。
- [x] **设置页信息补全（v1.3.2/1.3.3）**：显示本机 IP（含 Tailscale 100.x）与实际监听端口；Android 新增固定监听端口设置（占用回退）；设置分组「局域网配置 / 跨网络配置」；收件箱「浏览」改「更换目录」；手动设备区块内容改滚动区修裁切。
- [x] **息屏设备消失修复（v1.3.6）**：Android 前台服务持 WifiLock（API29+ 低时延模式）+ WAKE_LOCK；桌面手动设备探活容忍阈值翻倍。手动设备加备注与编辑（回填式），最近接收加清空按钮，标题栏设备数徽章移除。
- [x] **当前版本显示 + 检查更新 + 应用内更新（v1.3.7）**：双端设置页显示版本并可检查 GitHub 最新版；Windows 打包版下载 zip → 覆盖脚本 → 重启，Android 下载 APK 拉起安装器；覆盖安装免手动删旧版。
- [x] **检查更新 403 回退（v1.3.8）**：GitHub API 匿名限流按出口 IP 计（挂代理易 403），API 失败自动回退解析 `releases/latest` 重定向拿版本号（网页端无限流）；检查更新移入「应用行为」分组；加密口令「显示」改眼睛图标按钮。
- [x] **Android 固定发布签名（v1.3.9）**：此前 CI 发 `assembleDebug` 包、debug 签名每台构建机随机，导致用户覆盖安装报签名冲突（也使新图标一直装不上）。改用固定 release 密钥库（4 个 GitHub Secrets 注入），`build.gradle.kts` 有密钥库时走 release 签名、CI `assembleRelease`，本地实测 APK 证书 SHA-256 与密钥库一致。通知栏去掉多余大图标只留新墨洞小图标；检查更新「已是最新版」双端明确提示（桌面弹框 / Android Toast）。**用户须卸载旧版重装一次 v1.3.9，之后覆盖安装与应用内更新生效。**
- [x] **Android 通知彩色品牌图标（v1.3.10）**：常驻状态通知与收件通知增加完整的彩色 App 大图标；状态栏保留 Android 规范要求的单色小图标。
- [x] **Android 通知重复图标修复（v1.3.11）**：移除通知左侧额外的 `largeIcon`，避免与厂商通知模板在右侧显示的应用图标重复；最终只保留系统管理的新墨洞图标。
- [x] **Android 设置页信息与分组优化（v1.3.12）**：顶部改为「本机 / 版本 / 端口」三行并移除无需用户处理的本机 IP；配置项拆为「设备设置 / 传输安全 / 跨网络配置」，监听端口归入跨网络配置。
- [x] **Android 更新弹窗版本摘要（v1.3.13）**：明确显示当前版本、最新版本和更新可用状态；从 GitHub Release Markdown 中清理出最多 4 条简短变化，自动排除安装/下载/校验段落；无直接 APK 时改为进入发布页。
- [x] **Android 跨网络字段明确化（v1.3.14）**：将「对方 IP / 端口」改为「对方 Tailscale IP / 对方的监听端口」，同步区域说明和错误提示；两个输入框改为上下整行布局，避免窄屏长标签被截断。
- [x] **Android 传输术语与提示统一（v1.3.15）**：用户可见的「吞入 / 吐出」全部改为「发送 / 接收」，覆盖进度、完成状态、历史标题与通知；精简「轻点墨洞选择文件」和「还没发现设备」；本机端口增加「建议自定义固定端口」说明。
- [x] **Android 使用说明（v1.3.15）**：首次打开自动显示一次简洁说明，重点区分局域网与 Tailscale 跨网络连接；关闭后不再自动弹出，设置页保留「使用说明」按钮供随时查看。
- [x] **Android 稳定性终审（v1.3.16）**：修复同名接收覆盖、超大整块加密内存风险、半开连接占用、节点停止残留 Socket、多地址直连、NSD 自愈、MediaStore 导出误删、外部分享转屏重复处理、监听端口校验和更新 APK 包名/签名校验；补齐 11 项 Android 单元测试、Lint 与正式签名发布门禁。
- [x] **Android GitHub 入口（v1.3.17）**：设置页「帮助与更新」新增 GitHub 仓库按钮，使用系统浏览器打开项目主页，无法打开时给出明确提示。
- [x] **Android 跨网络设备列表布局修复（v1.3.18）**：已添加设备从本机监听端口与备注输入框之间移至「添加设备 / 保存设备」按钮下方，配置填写顺序更清晰，编辑与删除行为保持不变。
- [x] **三端体验同步与 Android 在线状态修复（v1.3.19）**：桌面端同步 Android 的设置与使用体验；Android 对自动发现和手动设备统一执行多地址 TCP 探活，断网或对端崩溃后及时清理残留设备并自动恢复发现；新增或保存跨网络设备后清空备注、IP 和监听端口三个输入框。
- [x] **跨网络草稿与 Windows 体验对齐（v1.3.20）**：Android 对方监听端口打开设置时不再预填上台设备常用值，添加、保存或删除正在编辑的设备后统一清空备注、Tailscale IP 和监听端口；Windows 保持桌面布局并对齐 Android 的状态文案、设置草稿、更新摘要、接收历史与设备恢复逻辑。
- [x] **Android 风格桌面设置与局域网在线判定（v1.3.21）**：Windows 设置内容和双栏布局不变，输入框改为 Android 同款描边浮动标签，使用说明、更新结果和校验提示改为主窗口内圆角模态面板；Android 自动发现设备只通过当前 WiFi/以太网同网段地址探活，不再因 Windows 同时通告 Tailscale 地址而在离开局域网后残留，手动跨网络设备保持原逻辑。
- [x] **传输取消、进度收尾与大文件优化（v1.3.22）**：Windows 与 Android 发送中可取消当前文件并清空等待队列；发送方关闭出站连接后，对端立即触发未完成结束事件、清除进度并删除 `.part`；Android 已知大小的 `content://` 文件改为直接流式发送，双端增加实时速度显示与高延迟链路 Socket 缓冲，Android 横竖屏重建后仍可继续取消当前发送。
- [x] **Android 设备在线状态修复（v1.3.23）**：对端下线或未开机时不再残留"在线"假象——新发现的 NSD 服务先 TCP 验证连通才入列（掐断 Android 13+ 系统 mDNS 缓存在重启发现时回灌陈旧记录的死循环）；手动设备取消启动乐观注册，改由探活循环首轮（立即执行）验证后上线；探活改设备级并行并支持回前台立即触发一轮核对；节点启动主动推送当前列表、Activity 恢复以节点实时列表为准，前台服务被杀重启后 UI 不再挂着旧设备；`currentLanLinks` 显式排除 VPN transport（Android VPN 网络会继承底层 WiFi transport）。
- [x] **大文件传输吞吐修复（v1.3.24）**：GB 级文件传输慢的根因是 v1.3.22 的 1MB socket 缓冲设置在连接建立之后——TCP 窗口缩放握手时已协商完，事后设置无效且禁用系统窗口自动调优，窗口被钉死 1MB（WiFi RTT 30–60ms 上限 17–33MB/s、DERP 中继仅 ~5MB/s）。双端缓冲提至 4MB 并挪到 bind/connect 之前（桌面监听 socket bind 前设置、发送走新 `_connect_transfer_socket`；Android ServerSocket bind 前、connectToPeer connect 前）；Android 导出到 Downloads 的复制缓冲 8KB→1MB（GB 级文件此前需数十万次读写，"进度走完还在转"）。回环基准明文 512MB 由 806MiB/s 提至 1780MiB/s。实测局域网上行 40–50MB/s、下行 60–70MB/s。
- [x] **状态栏速度可见与跨网探活容忍（v1.3.25）**：Android 传输状态改为「方向 百分比 · 速度 · 文件名」并允许两行——长文件名只截断名字，实时速度不再被挤成省略号；手动设备（Tailscale）探活超时双端放宽至 ~3s（空闲后懒惰唤醒/DERP 建链首次握手常超 1.2s，过紧会把在线的跨网设备判死或迟迟不上线）。排查记录：源码跑桌面端时 Windows 防火墙只放行了 python.exe 的 Public 配置文件，而 Tailscale 接口是 Private——跨网入站全被拦，需补 Private 放行规则（打包版 exe 规则独立，不受影响）。
- [x] **取消传输即时生效（v1.3.26）**：v1.3.24 的 4MB 发送缓冲让取消出现十几秒滞后——close/shutdown 会先把缓冲里已排队的数据按链路速度发完才断开，跨网中继（几百 KB/s）下对端要再"收"十几秒才看到中断。双端取消路径改为 SO_LINGER(0) 硬断开（RST 立即丢弃缓冲），接收端把 RST 归类为「接收中断」而非「接收失败」。实测跨网结论存档：同 WiFi 下两端 Tailscale 仍走 relay "hkg"（几百 KB/s），疑似本机代理 TUN（Mihomo）干扰 UDP 打洞；跨网速度受限于 DERP 中继属链路问题，非软件瓶颈。
- [x] **Tailscale 设备幽灵在线根治（v1.3.27）**：实测手机未开 Tailscale 时跨网设备仍一直显示在线——纯 TCP connect 探活被欺骗：发往 100.x 的连接按默认路由泄漏，被 Clash/Mihomo 等代理 TUN 本地假 accept（TUN 透明代理先完成本地三次握手再拨真实目标），或进运营商 CGNAT（100.64.0.0/10 正是 CGNAT 段）。双端修复：100.x 目标的探活与发送强制绑定"真正的 Tailscale 网络"——Android 找持有 100.64/10 地址的 TRANSPORT_VPN 网络（仅看 VPN transport 会误绑代理 TUN）用其 socketFactory；桌面绑定本机 Tailscale 接口 IP 为源地址；找不到即 Tailscale 未连接，立即判不可达。Tailscale 关闭后跨网设备在 1-4 轮探活内消失。
- [x] **Tailscale 离线秒剔与直连地址优先（v1.3.28）**：本机 Tailscale 不在线时纯 CGNAT 地址的手动设备"确定不可达"，双端跳过 4 轮抖动容忍立即剔除（此前关 Tailscale 后跨网设备还要挂 30 多秒，被感知为幽灵在线残余）；发送连接地址排序为局域网/直连优先、100.x 殿后——对端通告里 Tailscale 地址排前时，同 WiFi 传输曾可能误走 relay 把直连速度拖成几百 KB/s。桌面实测复核：对离线手机三种探活方式均正确超时，桌面探活链路本身无假 accept。
- [x] **监听端口建议范围提示（v1.3.29）**：双端设置页与 README 明确建议固定端口取 1024-49151——49152-65535 是系统临时端口动态区（Windows/Android 发起出站连接随机占用），固定监听落在该区有小概率开机被占触发回退随机端口，对端手动配置随之失配；示例端口从 52130 改为 41300。
- [x] **桌面小屏与设置视觉修复（v1.3.30）**：Windows/macOS 主窗口最小尺寸降至 720×480，低高度自动压缩墨洞区域；传输文件名与「百分比 · 实时速度」拆分显示，长文件名不再挤掉速度。桌面设置页改为圆角分组，宽屏双列、窄屏自动单列，并补齐悬停、开关、输入标签和页面切换过渡；macOS 使用说明弹窗显式绘制深色半透明背景，修复平台原生白底。
- [x] **桌面打包体积优化**：`inkhole-pet.spec` 在收集清单上过滤 WebEngine/Quick3D/Charts/Multimedia/Pdf 等未用重型 Qt 模块，解压后 412MB → 164MB（功能与 UI 不变，实测打包 exe 正常启动、QtQuick 桌宠正常渲染）。
- [x] **测试**：pytest 56 项、Android 16 项单元测试与 Lint 全通过；256MB 明文回环实测约 806.4MiB/s；Windows PyInstaller 成品已完成启动检查。

## 已完成 ✅（2026-07-19：v1.4.0 文件夹直传）

- [x] **WHF1 流式文件夹协议**：发送端只扫描一次并逐文件读取，不生成中间归档、不整包进内存；目录条目保留相对路径、文件/目录修改时间和空目录，文件夹加密固定使用 WHE2 分块流。
- [x] **能力协商与旧版兼容**：新增独立 WHPC 探测及 mDNS/NSD `caps=folder-v1` 通告；手动 IP/Tailscale 对端也能主动识别能力，旧客户端自动回退现有 ZIP 行为。
- [x] **原子接收与安全边界**：桌面和 Android 都先写隐藏暂存目录，完整校验后再改名提交；拒绝穿越、绝对路径、反斜杠、符号链接/联接/特殊文件、跨平台非法名称、大小写重名、文件与目录冲突、异常条目数和大小声明。
- [x] **Android 公共目录导出**：Android 10+ 用 MediaStore 按相对父目录逐文件写入 `Download/InkHole/<根目录>`，整批保持 pending、成功后统一发布、失败整批回滚；完全空目录受系统限制会忽略，其他目录结构保留。
- [x] **桌面与 Android 界面补全**：桌面增加文件夹选择入口，接收历史识别目录；Android 通知与历史显示文件夹语义。同时包含 v1.3.30 的桌面小屏速度、圆润设置页和 macOS 使用说明白底修复。
- [x] **测试**：Python `60 passed`；Android 20 项单元测试、Lint 与 release 构建通过。

## 已完成 ✅（2026-07-19：v1.4.1 桌面发送体验）

- [x] **文件与文件夹统一选择**：桌面端移除发送按钮的二级菜单，改为单一内容选择器；同一窗口可混选普通文件和文件夹，传输层自动选择 WHPP 或 WHF1，文件夹继续完整保留目录结构。
- [x] **主页墨洞视觉调整**：标准窗口下中央墨洞由 160×160 提升为 224×224，小窗口仍可自适应缩回，避免遮挡发送按钮、百分比和实时速度。
- [x] **测试**：Python `63 passed`；新增混合内容选择、单入口发送和主页墨洞尺寸回归测试。

## 已完成 ✅（2026-07-19：v1.4.2 三端加密开关）

- [x] **Windows / macOS**：传输安全设置新增独立端到端加密开关；关闭时禁用口令输入但保留口令，节点实际发送明文；重新开启后继续使用原口令。
- [x] **Android**：Compose 设置页增加同等开关并持久化到 `SharedPreferences`，前台服务仅在开关启用时向传输节点提供口令。
- [x] **旧配置兼容**：旧配置未保存开关状态时，有口令默认保持加密，无口令默认关闭，升级后不会静默改变原有传输方式。
- [x] **测试**：Python `67 passed`；Android 单元测试、Lint 与 release 构建通过，并覆盖保留口令但明文传输的回归路径。

## 已完成 ✅（2026-07-20：v1.5.0 三种跨网络方式）

- [x] **共享 Go 传输核心**：Windows/macOS 通过 JSON sidecar、Android 通过 gomobile AAR 复用同一实现；统一承载 Magic Wormhole、SSH 反向转发、SPAKE2、Noise IK 和 yamux。
- [x] **一次性短码**：自定义 AppID `com.rexvane.inkhole/transport-v1`；发送端生成短码/二维码，接收端先确认内容摘要；公共 Mailbox 与 Transit、直连优先、HTTP CONNECT 代理、取消和超时已打通。多文件复用同一会话的独立 WHPP/WHF1 流，不使用 ZIP 传输。
- [x] **SSH VPS 中继**：只需 SSH 地址、端口、用户名和已有私钥；主机指纹固定，反向端口仅监听 VPS 回环地址，断线指数退避重连。设备用一次性 SPAKE2 配对码交换 Noise 身份，默认外层端到端加密，可按设备关闭。
- [x] **私钥边界**：不生成 SSH 密钥。桌面粘贴私钥、口令和 Noise 私钥进入系统凭据存储；Android 使用 Keystore 加密存储，私钥文件使用持久化 SAF URI。普通配置只保留非敏感元数据。
- [x] **loopback 认证**：共享核心暴露的发送端点要求 `IKAT` 会话令牌；核心回注接收节点要求 `IKCI` 节点令牌；外部端点不参与局域网/Tailscale 探活。
- [x] **三端界面**：桌面和 Android 均保留 Tailscale，并增加一次性短码与 SSH 设置/配对；Android 提供跨网主页入口、二维码深链、内容确认和三页签设置。
- [x] **WHPP v3 可靠传输**：成功必须收到 `ACK_OK + SHA-256`；普通文件与 WHF1 文件夹、明文与 WHE2 加密均支持持久化断点续传，并用完成回执处理 ACK 丢失，避免重复落盘。Android 在 ACK 后、公共目录导出前被终止时，下次启动会恢复导出并按 `transfer_id` 合并接收历史。
- [x] **局域网设备身份**：桌面使用系统凭据存储、Android 使用 Keystore 保存 ECDSA P-256 私钥；WHPC 随机挑战和 WHPP 元数据均签名，选择设备后固定指纹，双端设置页支持查看和撤销信任。
- [x] **发布链路加固**：Windows 更新包校验 SHA-256、ZIP 路径与 Authenticode 同证书签名；Windows/macOS 发布工作流要求代码签名，macOS 完成 notarization、stapling 与 Gatekeeper 检查；标签发布等待 Windows、macOS、Android 全部成功后才一次性创建 Release。
- [x] **Wormhole 并发收尾**：取消 offer/bridge 与会话关闭不再在服务互斥锁内阻塞；context 超时或取消会主动关闭 yamux 与 loopback 监听端点，并增加取消竞态和 `go test -race` 回归测试。
- [x] **验证**：公共 Magic Wormhole 真实端到端传输成功；Python `113 passed`；Go race、vet、配对与隧道测试通过；Android 38 项单元测试、Lint 与 Debug APK 构建通过。macOS/Windows/Android 正式签名打包仍是发布门禁。

## 待办 📋

### 功能增强（可选）
- [x] 文件夹传输（v1.1.0 ZIP；v1.4.0 WHF1 流式直传并保留目录结构，旧版自动回退 ZIP）。
- [x] 手动添加设备 + Tailscale 跨网直连（桌面 v1.2.0，Android v1.3.0）。
- [x] 检查更新 + 应用内更新（双端 v1.3.7；403 回退 v1.3.8；Android 固定签名可覆盖安装 v1.3.9）。
- [ ] 多设备广播发送（同时发给多台设备）。
- [x] 一次性短码 NAT 穿透（v1.5.0：Magic Wormhole 直连优先、Transit 兜底、PAKE 配对）。
- [x] Android 端 Crypto/WHPP 与 Python 端固定向量互通测试，接入 CI（v1.5.0：WHPC 能力签名、WHPP 发送方签名与接收方回执签名使用三组共享 SHA-256 向量；`android/**` 或 `transport-core/**` 变化都会重建 AAR 并运行 Android 测试）。
- [x] Android 端 `onServiceLost` 后执行连续 TCP 探活，确认离线后再移除设备。
- [ ] 音效（可选）。

### 收尾
- [x] 大文件传输实测（256MB 明文回环约 806.4MiB/s；跨网络实际速度仍由直连或 DERP 中继链路决定）。

## 快速命令备忘

```bash
# 测试(P2P 引擎 + 手动设备 + 主窗口冒烟,无需真实显示器)
make test
# 或
PYTHONPATH=src QT_QPA_PLATFORM=offscreen python -m pytest tests/ -q

# 起桌宠
PYTHONPATH=src python -m inkhole.pet
PYTHONPATH=src python -m inkhole.pet --name 我的电脑 --secret 加密口令

# 命令行版 P2P（无 GUI，监视发件箱）
PYTHONPATH=src python -m inkhole.p2p \
    --inbox ~/InkHole/收件箱 --outbox ~/InkHole/发件箱

# 打包
cd packaging && build-windows.bat        # → packaging\dist\InkHolePet\ (onedir 目录，压 zip 发布)
cd packaging && bash build-mac.sh        # → packaging/dist/InkHolePet.app

# Git push（需走 Clash 代理）
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push
```

## 当前版本（v1.5.0）

三端保留局域网 mDNS 和 Tailscale，并增加 Magic Wormhole 一次性短码与 SSH VPS 长期中继。跨网连接由共享 Go 核心处理，文件仍使用 WHPP/WHF1；短码和 SSH 配对均使用 PAKE，SSH 设备再固定 Noise 身份。私钥只从文件读取或进入系统安全存储，不写普通配置、不由应用生成。三端继续支持流式文件夹、ACK、取消、实时速度、原子接收和后台接收。
